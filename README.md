# signal-aggregator

Ordinary public Signal contract for the `aggregator` component.

The contract asks the daemon to collect and normalize recent work evidence. It
carries source volumes, timestamps, locators, repository changes, transcript
segments, truncations, and read failures. It does not carry synthesized review
or judgment.
